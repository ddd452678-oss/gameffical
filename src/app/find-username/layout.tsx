import type { Metadata } from "next";

export const metadata: Metadata = { title: "아이디 찾기 — 겜피셜" };

export default function FindUsernameLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
